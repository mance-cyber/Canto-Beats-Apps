╔══════════════════════════════════════════════════════════════╗
║                  🎯 Canto-Beats 快速參考                     ║
╚══════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────┐
│  🚀 啟動應用程序                                              │
└──────────────────────────────────────────────────────────────┘

  方法 1（推薦）：
  ```bash
  cd /Users/nicleung/Public/Canto-Beats-Apps
  ./run_app.sh
  ```

  方法 2（手動）：
  ```bash
  cd /Users/nicleung/Public/Canto-Beats-Apps
  source venv/bin/activate
  python main.py
  ```

┌──────────────────────────────────────────────────────────────┐
│  🔍 環境檢查                                                  │
└──────────────────────────────────────────────────────────────┘

  檢查環境配置：
  ```bash
  ./venv/bin/python check_env.py
  ```

  驗證虛擬環境：
  ```bash
  which python
  # 應該顯示: .../Canto-Beats-Apps/venv/bin/python
  ```

┌──────────────────────────────────────────────────────────────┐
│  📋 查看日誌                                                  │
└──────────────────────────────────────────────────────────────┘

  查看最新日誌：
  ```bash
  tail -n 100 ~/.canto-beats/logs/canto-beats_*.log
  ```

  搜索錯誤：
  ```bash
  grep -i "error" ~/.canto-beats/logs/canto-beats_*.log
  ```

  在編輯器中打開：
  ```bash
  open ~/.canto-beats/logs/
  ```

┌──────────────────────────────────────────────────────────────┐
│  ⚠️  常見問題                                                 │
└──────────────────────────────────────────────────────────────┘

  **問題**：Splash screen 後崩潰
  **原因**：沒有在虛擬環境中運行
  **解決**：使用 `./run_app.sh` 或 `source venv/bin/activate`

  **問題**：提示「MPV 未安裝」
  **原因**：沒有在虛擬環境中運行
  **解決**：使用 `./run_app.sh` 或 `source venv/bin/activate`

  **問題**：缺少 PySide6、MLX 等模塊
  **原因**：沒有在虛擬環境中運行
  **解決**：使用 `./run_app.sh` 或 `source venv/bin/activate`

┌──────────────────────────────────────────────────────────────┐
│  🛠️  重新安裝依賴                                             │
└──────────────────────────────────────────────────────────────┘

  ```bash
  source venv/bin/activate
  pip install --upgrade pip
  pip install -r requirements.txt
  ```

┌──────────────────────────────────────────────────────────────┐
│  📚 文檔                                                      │
└──────────────────────────────────────────────────────────────┘

  • `README.md` - 項目概述
  • `docs/TROUBLESHOOTING.md` - 故障排除指南
  • `docs/SOLUTION_SUMMARY.md` - 問題解決總結

┌──────────────────────────────────────────────────────────────┐
│  ✅ 環境狀態檢查清單                                          │
└──────────────────────────────────────────────────────────────┘

  ☐ 在虛擬環境中（命令提示符有 (venv) 前綴）
  ☐ Python 路徑正確（.../venv/bin/python）
  ☐ 所有依賴已安裝（check_env.py 顯示全綠）
  ☐ AVPlayer 可用（macOS Apple Silicon）
  ☐ MLX Metal 可用（Apple Silicon 加速）

╔══════════════════════════════════════════════════════════════╗
║  💡 記住：永遠在虛擬環境中運行應用程序！                     ║
╚══════════════════════════════════════════════════════════════╝
